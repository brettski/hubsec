name: Master Push CI

on: 
  push:
    branches:
      - master

jobs:
  build:
    name: Build and test

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Use Node.js 10.x
      uses: actions/setup-node@master
      with:
        node-version: 10.x
    - name: npm install, build, and test
      run: |
        npm ci
        npm run build --if-present
        npm test
      env:
        CI: true
        BRETT_SECRET: ${{ secrets.BRETT_SECRET }}
    - name: Notification
      uses: 8398a7/action-slack@v2
      with:
        status: ${{ job.status }}
        author_name: Master Push CI
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SLACK_WEBHOOK_URL: ${{ secrets.SHOOK }}
      if: job.status == 'failure' || job.status == 'cancelled'


  something:
    name: Something else
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Use Node.js 10.x
      uses: actions/setup-node@master
      with:
        node-version: 10.x
    - name: npm install, build, and test
      run: |
        npm ci
        npm run build --if-present
        npm test
      env:
        CI: true
        BRETT_SECRET: ${{ secrets.BRETT_SECRET }}
   
    - name: Notification
      uses: 8398a7/action-slack@v2
      with:
        status: ${{ job.status }}
        author_name: Master Push CI
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SLACK_WEBHOOK_URL: ${{ secrets.SHOOK }}
      if: always()
      
  post:
    name: Post Done
    needs: build, something
    runs-on: ubuntu-latest
    steps:
    - name: get req
      run: curl https://hookops-q8h27n9ef.now.sh/api/goodbye
      